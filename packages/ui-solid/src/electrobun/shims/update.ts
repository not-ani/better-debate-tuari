import {
  checkForUpdates,
  getUpdateStatus,
  installUpdateNow,
  type DesktopUpdateStatus,
} from "../bridge";

export type { DesktopUpdateStatus };
export { checkForUpdates, getUpdateStatus, installUpdateNow };
