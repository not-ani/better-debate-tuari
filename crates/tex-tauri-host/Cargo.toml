[package]
name = "tex-tauri-host"
version = "0.1.0"
edition = "2021"

[dependencies]
tex-core = { path = "../tex-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2", features = [] }
